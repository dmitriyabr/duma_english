# Анализ: Скорость прироста мастерства — исследование vs реализация

Дата: 2026-02-09
Автор: Сравнительный анализ на основе "Скорость прироста мастерства в языковых графах знаний.md"

---

## Executive Summary

**Текущая система работает хорошо**, но **слишком оптимистична** по сравнению с исследованиями:
- Мы достигаем 70 баллов за **8-12 попыток**, исследования рекомендуют **10-12 для 60-70** и **12-15 для 80-90**
- N-CCR early verification происходит после **2 successes**, исследования рекомендуют **3-5 для лёгких**, **8-15 для сложных**
- **Нет явного reward за spacing** (распределённую практику), хотя это один из ключевых факторов удержания (85% через год vs 22% при cramming)

**Главный риск:** Ученики могут получить "verified + 70" на ноду, но забыть её через 2 недели из-за недостаточного числа распределённых встреч.

---

## 📊 Детальное сравнение

### 1. Модель обновления

| Аспект | Исследование (рекомендации) | Наша реализация | Оценка |
|--------|---------------------------|-----------------|--------|
| **Алгоритм** | EMA: `θ_k = α·θ_{k-1} + (1-α)·c_k` с α ≈ 0.8 | Beta: `α += w×score, β += w×(1-score)` с cap=12 | ✅ Оба плавные |
| **Цель порога** | 95% (T=0.95) | 70 из 100 + verified | ⚠️ Разные шкалы |
| **Правильных подряд для порога** | ~14 при α=0.8 | ~8-12 с cap=12 | ⚠️ Быстрее |
| **Ошибка обнуляет?** | Нет (EMA smoothing) | Нет (Beta smoothing) | ✅ Согласовано |
| **Uncertainty** | Нет (EMA простая) | Да (Beta даёт σ) | ✅ Плюс нашей |

**Анализ:**
- EMA проще, но Beta даёт uncertainty — это преимущество для diagnostic режимов
- Наш cap=12 делает обновления более "отзывчивыми", чем EMA с α=0.8
- При α=0.8 вес прошлых ответов падает медленнее: после 10 ответов первый ответ всё ещё влияет с весом 0.8^10 ≈ 0.107
- У нас при cap=12: после 10 evidence с weight=1 каждый, α+β=12 → каждое новое evidence перенормирует всю историю

### 2. Скорость роста (кейс: vocab node, все success)

**Симуляция с нашей системой** (effectiveWeight = 1.0, без streak/PFA):

| Попытка | α | β | Mean | Delta | Cumulative |
|---------|---|---|------|-------|------------|
| 0 | 1.0 | 1.0 | 50.0 | - | - |
| 1 | 2.0 | 1.0 | 66.7 | +16.7 | 1 |
| 2 | 3.0 | 1.0 | 75.0 | +8.3 | 2 |
| 3 | 4.0 | 1.0 | 80.0 | +5.0 | 3 |
| 4 | 5.0 | 1.0 | 83.3 | +3.3 | 4 |
| 5 | 6.0 | 1.0 | 85.7 | +2.4 | 5 |
| 6 | 7.0 | 1.0 | 87.5 | +1.8 | 6 |
| 7 | 8.0 | 1.0 | 88.9 | +1.4 | 7 |
| 8 | 9.0 | 1.0 | 90.0 | +1.1 | 8 |

**С cap=12** (после 11 evidence α+β=12, renormalize):
| 10 | 10.9 | 1.1 | 90.8 | +0.8 | 10 |
| 15 | 11.5 | 0.5 | **95.8** | +0.4 | 15 |

**С streak ×1.25 (2nd success):**
| 2 | 3.25 | 1.0 | **76.5** | +9.8 | 2 |
| 3 | 4.81 | 1.0 | 82.8 | +6.3 | 3 |

**Достижение 70:** За **2-3 попытки** с streak, **3-4 без streak**.

**Рекомендации исследования:**
- 1 правильный → 15-25 баллов ✅ (у нас 16.7)
- 3 правильных подряд → 40-50 ❌ (у нас 75-82 с streak)
- 5 в разных контекстах → 60-70 ❌ (у нас 85+)
- 8-10 с интервалами → 80-90 ✅ (у нас 88-90)
- 12-15 распределённых → 95-100 ✅ (у нас 95+)

**Проблема:** Мы слишком быстро растём в начале (2-5 попыток → 70+). Исследование рекомендует **10-12 встреч для продуктивного знания** и **20-30 для долгосрочного удержания**.

### 3. N-CCR Early Verification

**Исследование:**
- Оптимальное N для лёгких элементов: **3-5**
- Оптимальное N для сложных: **8-15**
- Общий диапазон: **2-8** (BKT сценарии), **8-17** (логистические модели)
- **Корреляция NCC и BKT очень высокая (0.74-0.99)** — оба объявляют mastery почти одновременно

**Наша реализация:**
```typescript
// N-CCR early verification: 2 direct successes in a row
if (nextStreak >= 2 && directSuccess) {
  activationStateAfter = "verified";
}
```

**Оценка:** ⚠️ **Слишком агрессивно**. 2 success — это минимум диапазона даже для лёгких элементов. Для grammar/LO (сложные конструкции) 2 недостаточно.

**Риск:** Ученик может случайно угадать или использовать конструкцию в простом контексте дважды, получить verified, но не владеть ей реально.

### 4. Spacing Effect (распределённая практика)

**Исследование (критически важно!):**
- Распределённая практика → **85% удержания через год**
- "Cramming" (зубрёжка) → **22% удержания через год**
- Нужно минимум **8-12 осмысленных встреч** для стабильной репрезентации
- **20-30 распределённых** для долгосрочного удержания
- Эффект spacing перестаёт расти после ~20 повторений

**Наша реализация:**
```typescript
// Decay учитывает время:
halfLife = base × repetitionBoost × reliabilityBoost
  base: vocab=14 days, grammar=21, lo=10
  repetitionBoost = 1 + log₂(evidenceCount+1) × 0.35

// Но НЕТ явного reward за spacing!
// Нет разницы между:
//   - 10 evidence за 1 день
//   - 10 evidence за 10 дней (по 1 в день)
```

**Оценка:** ⚠️ **Spacing не учитывается в весе evidence**. Decay компенсирует забывание, но не поощряет распределённую практику.

**Что нужно:** Добавить spacing bonus — если evidence пришёл через N дней после предыдущего, и N в разумном диапазоне (1-30 дней), увеличить effectiveWeight.

### 5. Промежуточные визуальные уровни

**Исследование (umimecesky.cz):**
- 4 уровня прогресс-бара: **50% → 80% → 95% → 98%**
- Поддерживает мотивацию через визуализацию постепенного прогресса

**Наша реализация:**
- activationState: `observed` → `candidate_for_verification` → `verified`
- Numeric mastery: 0-100 continuous
- Нет явных промежуточных "badges" или визуальных уровней

**Оценка:** ⚠️ **Нет gamification промежуточного прогресса**. Ученик видит число 35 → 42 → 51, но нет "Half-way!" milestone.

### 6. Decay (распад мастерства)

**Исследование:**
- HLR Duolingo: вероятность вспомнить = `2^(-Δ / half-life)`
- Strength падает со временем
- Критически важно для лексики (в отличие от процедурных навыков)

**Наша реализация:**
```typescript
decayedMastery = masteryMean × e^(-t / halfLife)
halfLife = base × (1 + log₂(count+1)×0.35) × reliabilityBoost
  base: vocab=14, grammar=21, lo=10
```

**Оценка:** ✅ **Реализовано хорошо**. Decay есть, с учётом:
- Типа ноды (vocab быстрее забывается)
- Количества повторений (больше → медленнее забывается)
- Reliability (high → медленнее забывается)

### 7. Bounded Memory (α+β cap)

**Исследование:**
- Нет прямых рекомендаций по cap
- EMA с α=0.8 естественным образом "забывает" старые ответы (0.8^N)

**Наша реализация:**
```typescript
const POSTERIOR_STRENGTH_CAP = 12;
if (α + β > 12) {
  scale = 12 / (α + β);
  α = α × scale;
  β = β × scale;
}
```

**Оценка:** ✅ **Хорошее решение**, но cap=12 делает систему более "отзывчивой", чем рекомендуемое EMA α=0.8.

**Эквивалент:** Cap=12 примерно эквивалентен EMA с α ≈ 0.85-0.9 (более быстрое забывание старых ответов).

---

## 🔧 Конкретные рекомендации по улучшению

### 1. Замедлить начальный рост (Приоритет: HIGH)

**Проблема:** 2-3 evidence → 70+ баллов. Исследование рекомендует 10-12 для 60-70.

**Решение A: Увеличить cap** (более консервативно):
```typescript
const POSTERIOR_STRENGTH_CAP = 18; // было 12
```
- Эффект: Каждое новое evidence весит меньше относительно истории
- При cap=18: 70 баллов достигается за ~12-15 evidence
- Более aligned с исследованием (10-12 встреч для продуктивного знания)

**Решение B: Adjust initial prior** (более тонко):
```typescript
// Вместо α=1, β=1 (mean=50, uncertainty high)
const priorStrength = 6; // вместо 4
alphaBefore = previousScore01 × priorStrength; // было priorStrength
betaBefore = (1 - previousScore01) × priorStrength;
```
- Эффект: "Более уверенный" prior → медленнее меняется
- Но с cap=12 всё равно быстро достигнется cap

**Решение C: Graduated weighting** (самое точное):
```typescript
function effectiveWeightWithProgression(baseWeight: number, currentMean: number): number {
  // Раннее знание (0-50): базовый вес × 0.7
  // Среднее (50-70): базовый вес × 1.0
  // Позднее (70-90): базовый вес × 1.3 (чтобы "закрыть" ноду)
  if (currentMean < 50) return baseWeight × 0.7;
  if (currentMean < 70) return baseWeight × 1.0;
  return baseWeight × 1.3;
}
```

**Рекомендация:** Комбинация A+C — увеличить cap до 16-18 + graduated weighting.

### 2. Увеличить N-CCR порог (Приоритет: HIGH)

**Проблема:** 2 direct success → verified слишком агрессивно.

**Решение:**
```typescript
// Дифференцировать по сложности ноды
function nCcrThreshold(node: { type: string; gseCenter: number }): number {
  // Vocab (простые): 3-4
  if (node.type === "GSE_VOCAB") {
    return node.gseCenter < 40 ? 3 : 4;
  }
  // Grammar (средние): 4-5
  if (node.type === "GSE_GRAMMAR") {
    return node.gseCenter < 50 ? 4 : 5;
  }
  // LO (сложные): 5-6
  return node.gseCenter < 50 ? 5 : 6;
}

// В applyEvidenceToStudentMastery:
const nCcrRequired = nCcrThreshold(node);
if (nextStreak >= nCcrRequired && directSuccess) {
  activationStateAfter = "verified";
}
```

**Эффект:**
- Простое vocab слово "cat" (gseCenter=20) → verified за 3 success
- Сложная grammar "Can use past perfect continuous" (gseCenter=65) → verified за 5 success
- Более aligned с исследованием (3-5 для лёгких, 8-15 для сложных)

**Альтернатива:** Фиксированный порог 4 для всех (компромисс между 2 и 8).

### 3. Добавить Spacing Bonus (Приоритет: MEDIUM)

**Проблема:** Нет reward за распределённую практику.

**Решение:**
```typescript
function spacingBonus(
  lastEvidenceAt: Date | null,
  now: Date,
  evidenceCount: number
): number {
  if (!lastEvidenceAt || evidenceCount < 2) return 1.0;

  const daysSinceLast = (now.getTime() - lastEvidenceAt.getTime()) / (1000*60*60*24);

  // Optimal spacing window: 1-30 days
  // Too soon (<12 hours): no bonus (cramming)
  // Sweet spot (1-14 days): +20% bonus
  // Long gap (15-30 days): +10% bonus (reviewed before forgetting)
  // Very long gap (30+ days): no bonus (forgot, relearning)

  if (daysSinceLast < 0.5) return 1.0;  // < 12 hours
  if (daysSinceLast < 1) return 1.05;   // 12-24 hours: small bonus
  if (daysSinceLast < 7) return 1.2;    // 1-7 days: optimal
  if (daysSinceLast < 14) return 1.2;   // 1-2 weeks: still good
  if (daysSinceLast < 30) return 1.1;   // 2-4 weeks: reviewed in time
  return 1.0;                            // 30+ days: no bonus
}

// В applyEvidenceToStudentMastery:
effectiveWeight = baseWeight × conf × rel × PFA × streak × spacingBonus(...)
```

**Эффект:**
- 10 evidence за 1 день → effectiveWeight ≈ 10×1.0 = 10
- 10 evidence за 20 дней (по 1 в 2 дня) → effectiveWeight ≈ 10×1.2 = 12 (+20%)
- Поощряет длительную распределённую практику
- Aligned с исследованием (85% retention through spacing)

### 4. Промежуточные визуальные уровни (Приоритет: LOW, UX)

**Решение:**
```typescript
function masteryLevel(score: number): {
  level: "beginner" | "developing" | "proficient" | "mastered";
  progress: number; // 0-1 within level
  nextThreshold: number;
} {
  if (score < 50) return {
    level: "beginner",
    progress: score / 50,
    nextThreshold: 50
  };
  if (score < 70) return {
    level: "developing",
    progress: (score - 50) / 20,
    nextThreshold: 70
  };
  if (score < 90) return {
    level: "proficient",
    progress: (score - 70) / 20,
    nextThreshold: 90
  };
  return {
    level: "mastered",
    progress: Math.min(1, (score - 90) / 10),
    nextThreshold: 100
  };
}

// В UI: показать badge "Developing!" когда score пересекает 50
```

**Эффект:**
- Мотивация через milestones
- Ученик видит: "35 → 42 → **50 🎉 Developing!** → 58 → ..."

### 5. Дифференцировать сложность нод (Приоритет: MEDIUM)

**Проблема:** Все ноды растут с одинаковой скоростью (при одинаковом weight).

**Решение:**
```typescript
function difficultyMultiplier(node: { type: string; gseCenter: number }): number {
  // Простые ноды (gseCenter < 40): базовая скорость
  // Средние (40-60): 0.9× (чуть медленнее)
  // Сложные (60+): 0.8× (медленнее)

  if (node.gseCenter < 40) return 1.0;
  if (node.gseCenter < 60) return 0.9;
  return 0.8;
}

// В applyEvidenceToStudentMastery:
effectiveWeight = baseWeight × conf × rel × PFA × streak × difficultyMultiplier(node)
```

**Эффект:**
- Простое vocab "cat" достигает 70 за ~10 evidence
- Сложная grammar "Can use cleft sentences" достигает 70 за ~15 evidence
- Более realistic expectations

### 6. Track Encounter Diversity (Приоритет: LOW)

**Идея:** Учитывать разнообразие контекстов использования.

**Решение:**
```typescript
// В spacingStateJson добавить:
{
  "incidentalTaskTypes": ["qa_prompt", "role_play", "speech_builder"], // 3 типа
  "incidentalConfidences": [0.72, 0.8, 0.75],
  "directSuccessStreak": 2,
  "contextDiversity": 0.75  // NEW: 0-1, чем больше типов → выше
}

function contextDiversityBonus(taskTypeCount: number, totalEvidence: number): number {
  // Больше разных task types → лучше
  const diversity = Math.min(1, taskTypeCount / Math.min(5, totalEvidence));
  return 1 + diversity × 0.1; // до +10% за разнообразие
}
```

**Эффект:**
- Использовал слово в 5 разных контекстах → +10% к весу
- Aligned с исследованием (разнообразие контекстов важно для усвоения)

---

## 📈 Симуляция: До и После

### Кейс: Vocab node "conversation" (gseCenter=40)

**Текущая система (cap=12, N-CCR=2):**
| Evidence | Weight | α | β | Mean | State | Comment |
|----------|--------|---|---|------|-------|---------|
| 0 | - | 1.0 | 1.0 | 50.0 | observed | Start |
| 1 | 1.0 | 2.0 | 1.0 | 66.7 | observed | +16.7 |
| 2 (direct) | 1.25 | 3.25 | 1.0 | **76.5** | **verified** | +9.8, streak, N-CCR=2 |
| 5 | 1.0 | 6.25 | 1.0 | 86.2 | verified | +2.9 |
| 10 | 1.0 | 10.9 | 1.1 | 90.8 | verified | +0.8 (cap) |

**Достижение 70:** 2 evidence (1 день)
**Verified:** 2 evidence (1 день)

**С улучшениями (cap=18, N-CCR=4, spacing bonus, difficulty×0.95):**
| Evidence | Days | Spacing | Weight | α | β | Mean | State |
|----------|------|---------|--------|---|---|------|-------|
| 0 | 0 | - | - | 1.0 | 1.0 | 50.0 | observed |
| 1 | 0 | 1.0 | 0.95 | 1.95 | 1.0 | 66.1 | observed |
| 2 | 1 | 1.2 | 1.14 | 3.09 | 1.0 | 75.6 | observed |
| 3 | 3 | 1.2 | 1.14 | 4.23 | 1.0 | 80.9 | observed |
| 4 (direct) | 5 | 1.2 | 1.43 | 5.66 | 1.0 | 85.0 | **verified** |
| 8 | 15 | 1.2 | 1.14 | 10.2 | 1.0 | 91.1 | verified |
| 12 | 30 | 1.1 | 1.04 | 15.4 | 1.0 | 93.9 | verified |

**Достижение 70:** 3 evidence (4 дня)
**Verified:** 4 evidence (9 дней)
**Достижение 90:** 8 evidence (15 дней)

**Итог:**
- Медленнее (но realistic: 10-12 встреч для продуктивного знания ✅)
- Spacing поощряется (20% bonus за 1-7 дней между evidence ✅)
- N-CCR=4 более conservative (не 2 случайных успеха ✅)

---

## 🎯 Приоритизация изменений

### Must Have (реализовать сейчас)
1. **Увеличить N-CCR порог до 3-4** — слишком рискованно verified за 2 success
2. **Увеличить cap до 16-18** — текущий 8-12 evidence для 70 слишком быстро

### Should Have (ближайшие спринты)
3. **Spacing bonus** — критически важен по исследованию (85% vs 22% retention)
4. **Difficulty multiplier** — сложные ноды должны расти медленнее

### Nice to Have (будущие итерации)
5. **Graduated weighting** — early learning медленнее, late learning быстрее
6. **Промежуточные визуальные уровни** — UX мотивация
7. **Context diversity bonus** — поощрять разнообразие

---

## 📚 Ключевые источники

- Pelánék & Říhák (2017) — EMA с α=0.8, T=0.95, ~14 правильных подряд
- Nation (2013) — 10-12 встреч для усвоения лексики
- Settles & Meeder (2016) — HLR Duolingo, spacing критичен
- Teng (2016) — 14 встреч для рецептивного, 18+ для продуктивного
- Uchihara et al. (2019) — мета-анализ, 20-30 распределённых для долгосрочного

---

## Next Steps

1. **Decision:** Согласовать с командой приоритеты (Must Have vs Should Have)
2. **Backfill:** После изменения cap/N-CCR, пересчитать mastery для существующих учеников? (или только forward)
3. **A/B Test:** Spacing bonus можно A/B тестировать (экспериментальная группа с bonus vs контроль)
4. **Metrics:** Добавить tracking "time between evidence" в pipeline-debug для анализа
